function bouncer(arr) {
  // Don't show a false ID to this bouncer.
  function testIfTrue(value,index,array){
    return Boolean(value);
  };
  var result = arr.filter(testIfTrue);
  return result;
}

bouncer([false, null, 0, NaN, undefined, '']);
